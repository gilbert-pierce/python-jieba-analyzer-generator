编译过程里遇到的错误汇总
tauri-apps/tauri-action@v1 找不到

现象：Unable to resolve action tauri-apps/tauri-action@v1
原因：v1 这个 tag 当时并未发布（可用的是 action-v0.6.2）
处理：改用 tauri-apps/tauri-action@action-v0.6.2
tauri-action@action-v0.6.2 不支持你填的 input

现象：Unexpected input(s) 'uploadWorkflowArtifacts', 'workflowArtifactNamePattern'
原因：这些是新版文档/接口的参数，action-v0.6.2 没有
处理：移除这些 input，改用 actions/upload-artifact@v4 上传产物
CI 调用 npm run tauri build 但项目里没有 tauri script

现象：npm error Missing script: "tauri"
原因：v0 的 tauri-action 默认用 npm run tauri ...
处理：在 package.json 增加 "tauri": "tauri"
Windows MSI（WiX）打包失败：light.exe

现象：failed to run ... WixTools314\light.exe（在生成 .msi 时）
原因：WiX 工具链在 runner 环境执行失败（MSI 路线不稳定）
处理：把 src-tauri/tauri.conf.json 的 bundle 目标改成只产出 NSIS："targets": ["nsis"]（生成 setup.exe）
Artifacts 上传路径不对导致“找不到文件”

现象：No files were found with the provided path: src-tauri/target/release/bundle/**
原因：Windows 目标编译产物在 src-tauri/target/x86_64-pc-windows-msvc/release/bundle/**
处理：改成通配路径 src-tauri/target/**/release/bundle/**
Windows 运行后“导出分析结果”不可用

现象：exe 能跑但点击“导出分析结果”失败/无反应
原因：前端用 XLSX.writeFile() 走浏览器下载机制，Tauri Windows WebView 下不可靠
处理：改为 Tauri 环境走 保存对话框 + 本地写文件（dialog + fs 插件），并补齐权限配置；浏览器环境仍保留原下载方式
（附带）TypeScript lint 被 src-tauri/target 生成物干扰

现象：tsc 报“文件像二进制”等错误
原因：tsc 扫描到了 Tauri/Rust 构建输出目录
处理：tsconfig.json 增加 include/exclude，排除 src-tauri/**、dist/** 等目录

现象：actions/checkout@v4 阶段报错：Error: error: invalid path 'migrated_prompt_history/prompt_2025-12-03T04:00:13.187Z.json'，git.exe 退出码 128，尚未执行编译。GitHub Actions（windows-latest）checkout 失败：invalid path
原因：文件名中含 ISO 时间里的冒号（如 T04:00:13 中的 :）。Windows 文件名禁止使用字符 :，而 Linux/macOS 允许此类路径入库，导致仅在 Windows runner 上检出失败
处理：从仓库移除含非法文件名的目录或改名（时间中的冒号可改为 - 或 _）；将仅本地需要的目录加入 .gitignore，避免再次提交

现象：编译 app 时 tauri-build/embed-resource 调用 rc.exe：error RC2176 : old DIB in ...\src-tauri\icons\icon.ico; pass it through SDKPAINT，RC.EXE failed to compile specified resource file，cargo 退出码 101。Windows 资源编译失败：RC2176 old DIB（icon.ico）
原因：icon.ico 内位图格式过旧或与当前 Windows SDK 的 rc.exe 不兼容（俗称 old DIB）；若源 PNG 文件头损坏（非标准 89 50 4E 47 开头），也会导致生成/使用的 ICO 异常
处理：使用一张合法 PNG（建议 1024×1024）执行 npx tauri icon <源图.png> -o src-tauri/icons 重生成全套图标；提交前用 file/hex 确认 PNG 为有效二进制

现象：打包后的 exe 在无网络环境下界面样式丢失或异常。离线运行仍请求外网（非 CI 报错，属运行时/打包问题）
原因：index.html 使用 cdn.tailwindcss.com、fonts.googleapis.com、importmap+esm.sh 等外链；样式与字体依赖网络
处理：用 Tailwind 构建链（如 tailwindcss + @tailwindcss/vite）在构建期生成 CSS；移除外链字体，改用系统字体栈；业务依赖由 Vite 打入 bundle；Tauri 打包时 vite 设置 base 为相对路径（如 TAURI_PLATFORM 存在时用 ./）

